<!DOCTYPE html>
<html>
  <head>
    <title> Halfway | A mi-chemin du bonheur </title>
    <meta name="description" content="Trouvez des idées de dates dans l'entre-deux">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1.0, user-scalable=0, shrink-to-fit=no"/>
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-capable" content="yes">

    <%= render 'shared/ios_pwa_links' %>

    <%= favicon_link_tag 'icons/favicon.ico'%>

    <meta property="og:title" content="Halfway | A mi-chemin du bonheur" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="<%= request.original_url %>" />
    <meta property="og:image" content=<% image_tag("halfway_logo.png") %> />
    <meta property="og:description" content="Trouvez des idées de dates dans l'entre-deux" />
    <meta property="og:site_name" content="Halfway | A mi-chemin du bonheur" />

    <meta name="twitter:card" content="logo for Halfway"/>
    <meta name="twitter:title" content="Halfway | A mi-chemin du bonheur"/>
    <meta name="twitter:description" content="Trouvez des idées de dates dans l'entre-deux"/>
    <meta name="twitter:image:src" content=<% image_tag("halfway_logo.png")%> />

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <link rel="stylesheet" type="text/css" href="https://npmcdn.com/flatpickr/dist/themes/dark.css">

    <% if true %>
      <link rel="manifest" href="<%= asset_path 'manifest.json' %>">
    <% end %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="body position-relative">
    <%#= render "shared/flashes" %>
    <%= render "shared/shared_header" unless action_name == "show" && controller_name == "friends"%>

    <%= yield %>

    <% if controller_name == 'pages' && action_name == 'landing_page' %>
      <%= render "layouts/landing_navbar" %>
      <%= render "layouts/landing_footer" %>
    <% elsif action_name != "show" || controller_name != "friends"%>
      <%= render "shared/shared_navbar" %>
    <% end %>
  </body>
</html>
