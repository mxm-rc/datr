<div class="container">
  <h1>Ajoute ton date</h1>

  <% @users.each do |user| %>
    <% if current_user.accointances.status_exist(user) %>
      <%= render partial: "status_exist", locals: { user: user} %>
      <% else %>
      <%= render  partial: "add_button",  locals: { user: user} %>
    <% end %>
  <% end %>
</div>
